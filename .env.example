# Direct (non-pooled) Neon endpoint. Used by cmd/migrate for schema changes.
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx.REGION.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Pooled Neon endpoint (host contains "-pooler"). Used by cmd/api.
DATABASE_URL_POOLED="postgresql://USER:PASSWORD@ep-xxx-pooler.REGION.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Redis for the redirect cache and the shared rate limiter. Use rediss:// for TLS.
# Compose overrides this to point at its own Redis container.
REDIS_URL="redis://localhost:6379/0"

# Bearer token required by DELETE /{code}. At least 32 characters.
# Generate one with: openssl rand -hex 32
ADMIN_TOKEN="change-me"

# Optional overrides, shown with their defaults.
# PUBLIC_BASE_URL="http://localhost:8080"
# REDIS_TIMEOUT="100ms"         # per Redis call; raise for a remote Redis
# CACHE_TTL="10m"
# RATE_LIMIT_PER_MINUTE="10"
# RATE_LIMIT_BURST="5"
# CLIENT_IP_HEADER=""            # e.g. "Fly-Client-IP"; only behind a proxy that sets it
# EXPIRY_SWEEP_INTERVAL="1m"
# HTTP_ADDR=":8080"
# HTTP_READ_HEADER_TIMEOUT="5s"
# HTTP_READ_TIMEOUT="10s"
# HTTP_WRITE_TIMEOUT="10s"
# HTTP_IDLE_TIMEOUT="60s"
# HTTP_SHUTDOWN_TIMEOUT="15s"
# DB_MAX_OPEN_CONNS="10"
# DB_MAX_IDLE_CONNS="10"
# DB_CONN_MAX_LIFETIME="30m"
# DB_CONN_MAX_IDLE_TIME="5m"
# LOG_LEVEL="info"

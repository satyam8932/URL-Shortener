// Package repository is the persistence layer. It wraps the generated ent
// client, translates ent results into model types and ent errors into model
// errors, and is the only package that knows the data lives in Postgres.
package repository

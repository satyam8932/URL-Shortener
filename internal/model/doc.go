// Package model defines the domain types and domain errors shared across
// layers (for example Link, ErrNotFound). It depends on nothing else in the
// module, so handlers and services never need to import generated ent types.
package model
